/* ===== RÉINITIALISATION ===== */
* {
    margin: 0;
    padding: 0;
    box-sizing: border-box;
}

::selection {
    background: rgba(0, 255, 255, 0.3);
    color: var(--text-primary);
}

html {
    scroll-behavior: smooth;
}

/* ===== STYLES DE BASE ===== */
body {
    background: var(--gradient-bg);
    color: var(--text-primary);
    font-family: var(--font-body);
    line-height: 1.6;
    min-height: 100vh;
    overflow-x: hidden;
    position: relative;
}

/* Effet de scanlines */
body::before {
    content: '';
    position: fixed;
    top: 0;
    left: 0;
    width: 100%;
    height: 100%;
    background: linear-gradient(
        to bottom,
        transparent 50%,
        rgba(0, 255, 255, 0.03) 50%
    );
    background-size: 100% 4px;
    pointer-events: none;
    z-index: 9999;
    opacity: 0.3;
    animation: scanline 8s linear infinite;
}

@keyframes scanline {
    0% { transform: translateY(0); }
    100% { transform: translateY(100vh); }
}

/* ===== HEADER PRINCIPAL ===== */
header {
    min-height: 100vh;
    display: flex;
    flex-direction: column;
    align-items: center;
    justify-content: center;
    position: relative;
    padding: var(--container-padding);
    overflow: hidden;
}

/* Background animé */
header::before {
    content: '';
    position: absolute;
    top: 0;
    left: 0;
    width: 100%;
    height: 100%;
    background: 
        radial-gradient(circle at 20% 50%, rgba(0, 255, 255, 0.1) 0%, transparent 50%),
        radial-gradient(circle at 80% 20%, rgba(255, 0, 255, 0.1) 0%, transparent 50%),
        radial-gradient(circle at 40% 80%, rgba(57, 255, 20, 0.1) 0%, transparent 50%);
    animation: gradientShift 10s ease-in-out infinite alternate;
    z-index: -1;
}

@keyframes gradientShift {
    0% { transform: scale(1) rotate(0deg); }
    100% { transform: scale(1.1) rotate(180deg); }
}

/* ===== SECTION IDENTITÉ ===== */
.header-identity {
    text-align: center;
    margin-bottom: 4rem;
    position: relative;
    z-index: 1;
}

/* Conteneur photo avec effet néon */
.header-fig {
    position: relative;
    display: inline-block;
    margin-bottom: 2rem;
}

.header-fig::before {
    content: '';
    position: absolute;
    top: -10px;
    left: -10px;
    right: -10px;
    bottom: -10px;
    background: var(--gradient-neon);
    border-radius: 50%;
    filter: blur(15px);
    opacity: 0.5;
    animation: pulseBorder 3s ease-in-out infinite;
    z-index: -1;
}

@keyframes pulseBorder {
    0%, 100% { opacity: 0.3; transform: scale(1); }
    50% { opacity: 0.6; transform: scale(1.05); }
}

.header-img {
    width: 200px;
    height: 200px;
    border-radius: 50%;
    object-fit: cover;
    border: 3px solid transparent;
    background: linear-gradient(var(--bg-card), var(--bg-card)) padding-box,
                var(--gradient-neon) border-box;
    box-shadow: 
        0 0 30px var(--neon-cyan),
        0 0 60px var(--neon-pink),
        inset 0 0 20px rgba(0, 0, 0, 0.5);
    transition: all var(--transition-medium);
    cursor: pointer;
    position: relative;
}

.header-img:hover {
    transform: scale(1.05) rotate(5deg);
    box-shadow: 
        0 0 40px var(--neon-cyan),
        0 0 80px var(--neon-pink),
        0 0 120px var(--neon-green),
        inset 0 0 30px rgba(0, 0, 0, 0.7);
    filter: brightness(1.2) contrast(1.1);
}

.header-caption {
    margin-top: 1rem;
    font-family: var(--font-special);
    color: var(--text-secondary);
    font-size: var(--fz-small);
    letter-spacing: 2px;
    text-transform: uppercase;
    animation: flicker 3s infinite;
}

@keyframes flicker {
    0%, 100% { opacity: 1; }
    50% { opacity: 0.6; }
}

/* Titre principal */
.header-title {
    font-size: var(--fz-title);
    font-family: var(--font-heading);
    background: var(--gradient-neon);
    -webkit-background-clip: text;
    background-clip: text;
    color: transparent;
    text-shadow: 0 0 30px rgba(0, 255, 255, 0.5);
    margin: 1rem 0;
    position: relative;
    animation: glitch 5s infinite;
}

@keyframes glitch {
    0%, 92% { transform: translate(0); text-shadow: 0 0 30px rgba(0, 255, 255, 0.5); }
    93% { transform: translate(-2px, 2px); text-shadow: 0 0 30px var(--neon-pink); }
    94% { transform: translate(2px, -1px); text-shadow: 0 0 30px var(--neon-green); }
    95% { transform: translate(-1px, 1px); text-shadow: 0 0 30px var(--neon-cyan); }
    96% { transform: translate(1px, -2px); text-shadow: 0 0 30px var(--neon-pink); }
    97% { transform: translate(0); text-shadow: 0 0 30px rgba(0, 255, 255, 0.5); }
}

/* ===== NAVIGATION ===== */
.header-nav {
    width: 100%;
    max-width: 800px;
    text-align: center;
    position: relative;
    z-index: 1;
}

/* Navigation principale */
.nav-primary {
    margin-bottom: 2rem;
}

.nav-list {
    list-style: none;
    display: flex;
    justify-content: center;
    flex-wrap: wrap;
    gap: 2rem;
}

.nav-item {
    position: relative;
}

.nav-item a {
    color: var(--text-primary);
    text-decoration: none;
    font-family: var(--font-title);
    font-size: var(--fz-body);
    padding: 0.8rem 1.5rem;
    border: var(--border-glow);
    border-radius: var(--border-radius-sm);
    background: var(--bg-card);
    backdrop-filter: blur(10px);
    transition: all var(--transition-medium);
    display: block;
    position: relative;
    overflow: hidden;
    text-transform: uppercase;
    letter-spacing: 1px;
}

.nav-item a::before {
    content: '';
    position: absolute;
    top: 0;
    left: -100%;
    width: 100%;
    height: 100%;
    background: var(--gradient-neon);
    opacity: 0;
    transition: all var(--transition-medium);
    z-index: -1;
}

.nav-item a:hover {
    color: var(--text-primary);
    transform: translateY(-5px) scale(1.05);
    border-color: transparent;
    box-shadow: var(--shadow-neon-medium);
    text-shadow: var(--shadow-text);
}

.nav-item a:hover::before {
    left: 0;
    opacity: 0.2;
}

/* Lien ULCO spécial */
.nav-item:nth-child(3) a {
    background: linear-gradient(145deg, rgba(157, 0, 255, 0.2), rgba(0, 128, 255, 0.1));
    border-color: var(--neon-purple);
}

.nav-item:nth-child(3) a:hover {
    box-shadow: 0 0 30px var(--neon-purple);
}

/* Séparateur */
.nav-divider {
    width: 80%;
    height: 1px;
    background: var(--gradient-neon);
    border: none;
    margin: 2rem auto;
    opacity: 0.5;
}

/* Navigation secondaire */
.nav-secondary .nav-list {
    gap: 1rem;
}

.nav-secondary .nav-item a {
    font-size: var(--fz-small);
    padding: 0.6rem 1.2rem;
    background: rgba(255, 255, 255, 0.05);
    border-color: rgba(0, 255, 255, 0.2);
}

.nav-secondary .nav-item a:hover {
    background: rgba(0, 255, 255, 0.1);
    border-color: var(--neon-cyan);
}

/* Indicateur visuel au survol */
.nav-item a::after {
    content: '';
    position: absolute;
    bottom: 0;
    left: 50%;
    transform: translateX(-50%) scaleX(0);
    width: 80%;
    height: 2px;
    background: var(--gradient-neon);
    transition: transform var(--transition-medium);
}

.nav-item a:hover::after {
    transform: translateX(-50%) scaleX(1);
}

/* ===== SECTIONS SUIVANTES (À AJOUTER PLUS TARD) ===== */
section[id] {
    min-height: 100vh;
    padding: var(--container-padding);
    position: relative;
    scroll-margin-top: 2rem;
}

/* ===== RESPONSIVE ===== */
@media (max-width: 768px) {
    .nav-list {
        flex-direction: column;
        gap: 1rem;
    }
    
    .nav-item a {
        width: 100%;
        max-width: 300px;
        margin: 0 auto;
    }
    
    .header-title {
        font-size: clamp(2rem, 6vw, 3rem);
    }
    
    .header-img {
        width: 150px;
        height: 150px;
    }
}

@media (max-width: 480px) {
    :root {
        --container-padding: 15px;
    }
    
    .header-nav {
        padding: 0 10px;
    }
    
    .nav-secondary .nav-list {
        flex-direction: column;
    }
}

/* ===== EFFETS SPÉCIAUX ===== */
/* Particules animées en arrière-plan */
.particles {
    position: fixed;
    top: 0;
    left: 0;
    width: 100%;
    height: 100%;
    pointer-events: none;
    z-index: 0;
}

.particle {
    position: absolute;
    width: 2px;
    height: 2px;
    background: var(--neon-cyan);
    border-radius: 50%;
    animation: float 20s linear infinite;
}

.particle:nth-child(2n) {
    background: var(--neon-pink);
    animation-delay: -5s;
    animation-duration: 25s;
}

.particle:nth-child(3n) {
    background: var(--neon-green);
    animation-delay: -10s;
    animation-duration: 30s;
}

@keyframes float {
    0% {
        transform: translateY(100vh) translateX(0);
        opacity: 0;
    }
    10% {
        opacity: 1;
    }
    90% {
        opacity: 1;
    }
    100% {
        transform: translateY(-100px) translateX(100px);
        opacity: 0;
    }
}